<div class="card" data-id="<%= portfolio_item.id%>">
      <%= image_tag protfolio_img(portfolio_item.thumb_image, 'thumb') %> 

      <p class="card-text">
            <span> <%= link_to portfolio_item.title, protfolio_show_path(portfolio_item) %> </span>
            <span><%= portfolio_item.subtitle %> </span>
      </p>
</div>

      
