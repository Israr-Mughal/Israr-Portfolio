  <div class="col-sm-8 blog-main">
   
    <h2><%= @blog.title %></h2>
    <p><%= link_to 'Edit', edit_blog_path(@blog) if logged_in?(:site_admin)%> </p>
    <p><%= @blog.body %></p>

    <span><%= link_to 'Back', blogs_path  if logged_in?(:site_admin)  %></span>

  </div>

